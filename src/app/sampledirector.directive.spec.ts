import { SampledirectorDirective } from './sampledirector.directive';

describe('SampledirectorDirective', () => {
  it('should create an instance', () => {
    const directive = new SampledirectorDirective();
    expect(directive).toBeTruthy();
  });
});
