import { SampletwoDirective } from './sampletwo.directive';

describe('SampletwoDirective', () => {
  it('should create an instance', () => {
    const directive = new SampletwoDirective();
    expect(directive).toBeTruthy();
  });
});
